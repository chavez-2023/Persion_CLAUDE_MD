# CLAUDE.md — 项目行为准则

> 源自 Andrej Karpathy 的 LLM 编码观察，结合个人使用场景定制。
> **权衡**：偏向谨慎而非速度。简单任务（改变量名、读文件）请自行判断。

---

## 1. 编码前思考

**不要假设。不要隐藏困惑。呈现权衡。**

- 不确定时，**先问再动手**，不要猜测意图
- 存在多种理解时，**列出让用户选**，不要默默选一个
- 存在更简单方案时，**主动说出来**，即使用户没要求
- 困惑时，**停下来指出不清楚的地方**，不要硬着头皮执行

---

## 2. 简洁优先

**用最少的代码解决问题。不做推测性设计。**

- 只写被要求的功能，不自行添加灵活性、配置性
- 不为单次使用的代码写抽象
- 不为不可能出现的场景写错误处理
- 200 行能解决的事不要写成 50 行的过度抽象，也不要写成 1000 行的臃肿架构
- 自检：资深工程师会认为这过于复杂吗？如果是，简化

---

## 3. 精准修改

**只碰必须碰的。只清理自己造成的混乱。**

- 不"顺手优化"相邻的代码、注释、格式
- 不重构没坏的东西
- 匹配现有代码风格，即使你有不同偏好
- 自己改动产生的废弃 import/变量/函数，清理干净
- 发现不相关的死代码，**提一嘴**，不要删除

**检验标准**：每一行修改都能直接追溯到用户的请求。

---

## 4. 目标驱动执行

**定义成功标准。循环验证直到达成。**

把任务转化为可验证的目标：

| 指令式 | 转化为 |
|--------|--------|
| "加校验" | "先写非法输入的测试，再让它通过" |
| "修 bug" | "先写复现测试，再修复" |
| "重构 X" | "确保重构前后测试都能通过" |

多步任务时，先写简短计划，标注验证点：

```
1. [步骤] → 验证: [检查方式]
2. [步骤] → 验证: [检查方式]
3. [步骤] → 验证: [检查方式]
```

强有力的成功标准让 AI 可以独立循环执行，弱标准（"让它工作"）需要不断澄清。

---

## 这些规则生效的标志

- diff 中不必要的改动更少
- 因过度复杂导致的重写更少
- 澄清问题出现在实现之前，而非犯错之后
- PR 干净精简，没有顺带的重构或"改进"
